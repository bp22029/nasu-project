# 那須観光ルート提案アプリ — Claude Code 開発仕様書

> このファイルをプロジェクトルートに `CLAUDE.md` として置く。Claude Code は作業のたびに読むこと。
> 実装は **セクション9のプロンプトを上から順に、小さい単位で** 渡して進める。各段で動作確認してから次へ。

---

## 1. プロジェクト概要

- **目的**: 栃木県那須町の観光課題（デジタル観光体験の不足／若年層の取り込み不足）への提案アプリ。芝浦工業大学・システム工学特別演習 9班の成果物。
- **コンセプト**: 「Instagramで"見つける"」と「Google Mapsで"行く"」の間を埋める、那須町特化の観光ルート提案アプリ。
- **ターゲット**: 画像で情報収集する若年層。**車で那須を巡る観光客**を主対象とする（→ ルートは直線距離ではなく実際の道なり経路で提案する）。
- **アプリ名**: 未定（`NASU POT` は既存サービスと重複のため保留）。リポジトリ仮称 `nasu-tabi`。

---

## 2. 今回のスコープ（厳守）

**「必達ライン」のみを新規（スクラッチ）で実装する。** 既存モックHTMLは移植せず、Next.jsで作り直す。

今回やること:
- 機能1：ルート設計支援（画像選択 → **道なり**ルート提案）
- Google Places API による写真の動的取得
- 観光地データ（まず10〜15件で動かし、最終的に30件を目標）

**今回やらないこと**: 機能2（那須旅診断）、機能3（写真投稿）。ただし後で載せられるよう、セクション8の「拡張の差し込み口」だけは最初から用意しておく。

---

## 3. 技術スタック（確定）

| 区分 | 採用 | 備考 |
|---|---|---|
| フロント | Next.js 14 (App Router) + TypeScript | |
| スタイル | Tailwind CSS | |
| 地図 | **Leaflet + OpenStreetMap** | Google Mapsは使わない（理由はセクション5） |
| 経路探索 | **OpenRouteService (ORS)** `driving-car` | OSMベース。道なり経路・距離・時間 |
| 巡回順最適化 | **自前TSP**（全探索/最近傍） | ORSの距離行列を入力に使う |
| 写真 | **Google Places API (New)** | place_idで動的取得。地図外グリッドに表示 |
| バージョン管理 | **Git + GitHub** | セクション10のGit運用に従う |
| デプロイ（任意） | Vercel | |

予算は月1万円程度まで利用可。無料に固執しない。ORS公開インスタンスとPlacesは当面それで足りるが、上限に当たれば有料/セルフホストに切替可。

---

## 4. 地図上のルート描画（道なりの曲線）

- ORS Directions API（`driving-car`）で経路を取得する。**geometry は GeoJSON 形式で受け取る**（エンコード済みポリラインのデコード処理を避けるため）。
- 返ってきた経路は道路形状に沿った多数の座標点の連なり。これを Leaflet の `L.polyline` に渡すと、カーブを含む道なりの曲線として描画される（直線にしない）。
- ルート線は色つき・太線で見やすく（例: `color: "#2563eb", weight: 5, opacity: 0.85`）。各スポットには順番付きマーカーを置く。

---

## 5. 規約遵守ルール（重要・必ず守る）

**地図は Leaflet + OSM、経路は ORS、写真のみ Google。Google由来データを地図に載せない設計にする。**

理由: Google Maps Platform規約では「Places等のGoogleコンテンツを地図に表示する場合、その地図はGoogle Mapでなければならない」「競合地図サービスとの組み合わせ禁止」。よって地図側は完全にGoogle非依存（座標もOSM由来、経路もORS）に統一し、Googleは写真にのみ使い、写真は地図外のグリッドで表示する。

| データ | 出どころ | 保存可否 | 表示場所 |
|---|---|---|---|
| 緯度経度 | 国土地理院 or OSM(Nominatim) | 保存OK（Google由来でない） | 地図（Leaflet） |
| place_id | Google Places (Text Search) | **保存OK**（キャッシュ制限の例外） | 内部利用（写真取得キー） |
| 写真 | Google Places (New) | **保存禁止**。毎回動的取得 | 地図外グリッド |
| 経路形状 | ORS | 保存OK（Google由来でない） | 地図（Leaflet polyline） |

写真表示時の義務:
- `authorAttributions`（撮影者クレジット）が返ればそれを必ず表示する。
- Googleコンテンツを地図なしで表示するため、**Googleロゴ**を併記する。
- 写真本体・写真URLはキャッシュ／DB保存しない（place_idだけ保存）。

---

## 6. データ設計（spots.json）

```jsonc
{
  "id": "chausu",
  "name": "茶臼岳",
  "lat": 37.12,            // 国土地理院 or OSM 由来（Google由来にしない）
  "lng": 139.96,
  "placeId": "ChIJ...",   // Google Places の Text Search から取得（写真用）
  "tags": [],             // 機能2用に欄だけ確保。今は空でよい
  "description": "那須連山の主峰。ロープウェイで高山の眺望。"
}
```

- 座標は **Google Places由来にしない**（保存制限のため）。地理院地図/OSMから取る。
- placeId は Text Search のレスポンスから **place_id だけ抜き出して** 格納（他のコンテンツは保存しない）。
- `tags` は機能2（診断）用に空配列で用意しておくだけ。

**初期スポット候補**（現地調査・スライド既出）: 茶臼岳 / 鹿の湯 / 殺生石 / 那須高原りんどう湖ファミリー牧場 / チーズガーデン那須本店 / 南ヶ丘牧場 / 道の駅 那須高原友愛の森 / GOOD NEWS / 那須どうぶつ王国 / 那須ステンドグラス美術館 ほか、定番を補い10〜15件。

---

## 7. ルート設計の仕組み（2段構え）

1. ユーザーが画像グリッドでスポットを選択（推奨3〜8件）
2. 選択座標を **ORS Matrix API（driving-car）** に渡し、全ペアの**道なり距離・所要時間の行列**を取得
3. その行列を入力に **自前TSP** で巡回順を最適化（選択≤8は全探索、9以上は最近傍法）
4. 確定した順番で **ORS Directions API（driving-car, GeoJSON）** を呼び、道なりの経路形状を取得
5. 形状を Leaflet に色つき polyline で描画 ＋ 順番付きマーカー ＋ タイムライン（順・スポット名・所要時間）

距離・時間はORSが返す実測ベースを使う（ハバーサイン直線は使わない）。TSPアルゴリズム本体は自前で実装し、区間コストのみORSから供給する。

---

## 8. 将来の拡張の差し込み口（今のうちに用意）

機能1を作る時点で、以下を満たす構造にしておくこと。実装は不要、口だけ空けておく。

- **ルート計算を独立関数にする**: `計算(選択スポット配列) → ルート結果`。選択UIと計算を分離。
  → 機能2は「診断結果から選択配列を自動生成」してこの関数に渡すだけで載る。
- **写真取得を独立関数にする**: `写真取得(spotId) → 画像リスト`。取得元を内部で切替可能に。
  → 機能3は「投稿DBからの写真」をソースに追加するだけで載る（Places由来＋投稿由来の合成）。
- **spots.json に `tags` 欄を空で確保**（機能2のマッチング用）。

機能3は保存先DB（Supabase等）と最低限の投稿者管理が必要になり最も重い。今回は触らない。

---

## 9. Claude Code 着手プロンプト集（上から順に）

### プロンプト0 — プロジェクト基盤
```
Next.js 14 (App Router) + TypeScript + Tailwind CSS のプロジェクトを作成して。
src ディレクトリと App Router を使う設定にする。
地図表示に react-leaflet と leaflet を導入し、OpenStreetMap タイルで
那須町（中心 緯度37.07, 経度140.0 付近）を表示する最小ページを作って。
Leaflet は SSR で壊れやすいので、地図コンポーネントは dynamic import (ssr:false) で読み込むこと。
完成したら npm run dev での動作確認手順を教えて。
設計方針は CLAUDE.md に従う。
```

### プロンプト1 — 観光地データ
```
CLAUDE.md セクション6のスキーマに従い、観光地マスタの型(types/spot.ts)と
data/spots.json を作る使い捨てスクリプトを書いて。
- 緯度経度は OSM Nominatim でジオコーディングして取得（Google由来にしない、Nominatimは1req/秒・User-Agent必須）
- place_id は Google Places の Text Search から「place_id だけ」抜き出して格納（他のコンテンツは保存しない）
- tags は空配列、description は簡潔に
対象スポットは CLAUDE.md の初期候補リスト（10〜15件）。
APIキーは .env.local の GOOGLE_PLACES_API_KEY を使う。
生成後、地図上に1〜2件ピンを置いて座標が妥当か目視確認できるようにして。
```

### プロンプト2 — 画像グリッドUI
```
spots.json を読み込み、各スポットを画像カードのグリッドで表示する
React コンポーネントを作って。Instagram風の正方形グリッドで、
カードをタップすると選択状態がトグルされ、選択中は枠を強調する。
写真はこの段階では仮プレースホルダでよい（実画像はプロンプト5で差し替え）。
```

### プロンプト3 — 選択状態の管理
```
選択スポットを配列で管理する状態を実装して。
選択件数を画面下部に表示し、「ルートを設計する」ボタンを置く。0件のとき無効化。
このとき、ルート計算は「選択スポット配列を受け取る独立関数」として分離しておくこと
（CLAUDE.md セクション8の拡張方針）。
```

### プロンプト4 — 道なりルート（ORS Matrix → 自前TSP → ORS Directions → Leaflet）
```
CLAUDE.md セクション7の流れで道なりルート設計を実装して。
1) 選択座標を OpenRouteService の Matrix API(profile=driving-car)に渡し、
   道なりの距離・所要時間の行列を取得する。
2) その行列を入力に、巡回順を最適化する自前TSP関数を書く
   （選択8件以下は全探索、9件以上は最近傍法にフォールバック）。
3) 確定順で ORS Directions API(driving-car, geometry は GeoJSON で取得)を呼ぶ。
4) 取得した道なり形状を Leaflet に色つき polyline(color #2563eb, weight 5)で描画し、
   順番付きマーカーと、順・スポット名・所要時間のタイムラインを表示する。
ORS のキーは .env.local の ORS_API_KEY を使い、呼び出しは Next.js の API Route 経由にする。
```

### プロンプト5 — Places API 写真の動的取得
```
.env.local の GOOGLE_PLACES_API_KEY を使い、Next.js の API Route 経由で
Places API (New) を呼び、placeId からスポット写真を動的取得するエンドポイントを作って。
規約遵守: 写真本体はキャッシュせず毎回取得。レスポンスに authorAttributions を含め、
表示時に撮影者クレジットと Google ロゴを併記する。写真は地図ではなくグリッドに表示。
プロンプト2のプレースホルダを、この実画像に差し替えて。
写真取得は「spotId を受け取って画像を返す独立関数」にしておくこと（CLAUDE.md セクション8）。
```

---

## 10. 進め方の鉄則

1. プロンプトは1つずつ。各段で `npm run dev` で動作確認してから次へ。
2. AIが書いたコードの全体構造は人間が把握する。バグ修正の丸投げで無限ループにしない。
3. 今回のスコープは機能1のみ。機能2・3の中身は作り込まない（差し込み口だけ用意）。
4. APIキーは `.env.local` で管理。`.gitignore` に `.env*` を入れ、キーをチャットに貼らない。

### Git / GitHub 運用
- `main` は常に「動く状態」を保つ幹とする。
- **各プロンプト（ステップ）ごとに作業ブランチを切る**。例: `feature/00-base`, `feature/01-data`, `feature/04-route`。
- そのステップが `npm run dev` で動作確認できたら、commit して `main` にマージし、**push まで自動で行う**。
- commit メッセージは何をやったか分かる短い日本語/英語で（例: `feat: ORS道なりルート描画`）。
- **push してよいのは `npm run dev` で動作確認が取れたステップのみ**。動作未確認・ビルドが通らない状態のコードは push しない。
- 初心者のうちはブランチを切りすぎない。「各ステップで commit・push、ブランチは機能単位でゆるく」で十分。

---

## 11. 環境変数（.env.local）

```
GOOGLE_PLACES_API_KEY=（既存のキー。Places API (New) 有効化済み）
ORS_API_KEY=（openrouteservice.org で無料登録して発行）
```

`.gitignore` に `.env*` が含まれていることを必ず確認する。
